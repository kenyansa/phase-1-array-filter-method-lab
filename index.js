// Code your solution here
//  findMatching(): returns all drivers that match the passed in name:
    
// findMatching(): returns matching drivers if case does not match but letters do:
  
// findMatching(): returns an empty array if there is no match:
  
// fuzzyMatch(): returns a driver if beginning provided letters match:
    
// fuzzyMatch(): does not return drivers if only middle or ending letters match:
    
// fuzzyMatch():does not return drivers if only middle or ending letters match:
   
// matchName(): accesses the data structure to check if name matches:
